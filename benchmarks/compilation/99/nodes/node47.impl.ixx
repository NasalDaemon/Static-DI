module di.bench.compile.node47:impl;
import di.bench.compile.node47;

namespace di::bench::compile {

template<class Context>
int Node47::Node<Context>::impl(trait::Trait47::get) const
{
    return i + getNode(trait::trait46).get();
}

}
