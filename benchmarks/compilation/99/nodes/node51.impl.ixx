module di.bench.compile.node51:impl;
import di.bench.compile.node51;

namespace di::bench::compile {

template<class Context>
int Node51::Node<Context>::impl(trait::Trait51::get) const
{
    return i + getNode(trait::trait50).get();
}

}
